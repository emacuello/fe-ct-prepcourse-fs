// ⛔️ Recuerda que debes utilizar el objeto global "Math".

function elevarAlCubo(num) {
  // Retorna el valor de "num" elevado al cubo.
  // Tu código:
  let Elev = Math.pow(num, 3);

  return Elev;
}

module.exports = elevarAlCubo;
